import conditionalPluralize from 'dummy/utils/conditional-pluralize';
import { module, test } from 'qunit';

module('Unit | Utility | conditional pluralize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = conditionalPluralize();
  assert.ok(result);
});
