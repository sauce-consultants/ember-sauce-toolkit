import stkLogger from 'dummy/utils/stk-logger';
import { module, test } from 'qunit';

module('Unit | Utility | stk logger');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stkLogger();
  assert.ok(result);
});
